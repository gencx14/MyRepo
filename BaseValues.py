import cmath
class BaseValues:
    def __init__(self, pbase, vbase):
        self.pbase = Pbase
        self.vbase = Vbase
        self.zbase = self.vbase**2/self.pbase
        self.ybase = 1 / self.vbase

